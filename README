0))))))



Keeps commands executing in a loop.
You can change the delay in seconds between each command.
You can set a number of times for the command to execute.
Usage: looper command -d 3 -n 100.
Defaults: delay = 60 seconds, number of executions = infinite.
Call looper with no arguments to enter interactive mode.
